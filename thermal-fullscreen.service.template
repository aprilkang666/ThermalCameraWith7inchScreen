[Unit]
Description=Thermal Camera Fullscreen (ffplay)
After=graphical.target
Wants=graphical.target

[Service]
Type=simple
User={{USER}}
Environment=DISPLAY=:0
ExecStart={{INSTALL_DIR}}/scripts/start_thermal.sh
Restart=always
RestartSec=1

[Install]
WantedBy=graphical.target
